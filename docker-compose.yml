version: "3"
services:
  ml:
    image: "ml:tools"
    build:
      context: .
    stdin_open: true
    tty: true
    working_dir: /space
    volumes:
      - ${PWD}:/space
    ipc: host
    network_mode: host
